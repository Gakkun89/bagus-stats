<%= month_calendar(partial: 'calendars/calendar', events: @workouts) do |date, workouts| %>
  <%= date.day %>
  <% unless workouts.empty? %>
    <% workouts.each do |workout|  %>
      <%= workout.variety%>
    <% end %>
  <% end %>
<% end %>
