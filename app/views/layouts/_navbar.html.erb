<nav class="border-gray-200 bg-navbar-black">
  <div class="max-w-screen-xl flex flex-wrap items-center justify-center mx-auto p-4">
    <a href="https://flowbite.com/" class="flex flex-col items-center space-x-3 rtl:space-x-reverse">
        <%= image_tag("logo.webp", class: "h-36") %>
        <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">Bagus Stats</span>
    </a>

    <div class="hidden md:w-6/12 md:block ml-4" id="navbar-default">
      <%= form_with url: workouts_path, method: :get, data: { turbo_frame: 'workouts' } do |form| %>
        <label for="default-search" class="mb-2 text-sm font-medium text-gray-900 sr-only dark:text-white">Search</label>
        <div class="relative">
            <div class="absolute inset-y-0 start-0 flex items-center ps-3 pointer-events-none">
                <svg class="w-4 h-4 text-gray-500 dark:text-gray-400" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 20">
                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m19 19-4-4m0-7A7 7 0 1 1 1 8a7 7 0 0 1 14 0Z"/>
                </svg>
            </div>
             <%= form.search_field :q, class: 'form-control block w-full p-4 ps-10 text-sm text-gray-900 border border-gray-300 rounded-lg bg-gray-50 focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500', id: 'default-search' , placeholder: "Workout, Exercise, Participant..." %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
